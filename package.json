{
  "name": "bookmark-manager-burdenoff",
  "version": "1.0.0",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "bun test",
    "gendb": "prisma migrate deploy && prisma generate",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "6",
    "graphql": "^17.0.2",
    "graphql-yoga": "^5.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "eslint": "^10.9.0",
    "prisma": "6",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.24.0"
  }
}
